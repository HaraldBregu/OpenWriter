/**
 * Setup for renderer tests (jsdom environment), loaded after the test
 * framework is installed (`setupFilesAfterEach`). Currently a no-op.
 *
 * When renderer tests are added, attach shared mocks (e.g. window.api,
 * matchMedia, ResizeObserver) here so individual specs stay clean.
 */
export {};
