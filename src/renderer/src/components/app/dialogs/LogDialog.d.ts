interface LogDialogProps {
    open: boolean;
    onOpenChange: (open: boolean) => void;
}
export declare function LogDialog({ open, onOpenChange }: LogDialogProps): import("react/jsx-runtime").JSX.Element;
export {};
