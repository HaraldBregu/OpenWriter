import React, { type ReactNode } from 'react';
interface ProviderProps {
    readonly children: ReactNode;
}
export declare function Provider({ children }: ProviderProps): React.JSX.Element;
export {};
