import { type ReactElement, type ReactNode } from 'react';
interface FilesProviderProps {
    readonly children: ReactNode;
}
export declare function Provider({ children }: FilesProviderProps): ReactElement;
export {};
