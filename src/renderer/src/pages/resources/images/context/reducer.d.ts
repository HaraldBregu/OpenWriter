import type { FilesReducerState } from './state';
import type { FilesAction } from './actions';
export declare function filesReducer(state: FilesReducerState, action: FilesAction): FilesReducerState;
