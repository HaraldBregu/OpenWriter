/**
 * Polyfills loaded before the renderer test environment is set up.
 *
 * Currently a no-op — kept as the documented hook for adding browser-API
 * shims (TextEncoder, structuredClone, ResizeObserver, etc.) when renderer
 * tests start needing them.
 */
export {};
