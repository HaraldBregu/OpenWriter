interface CronDialogProps {
    open: boolean;
    onOpenChange: (open: boolean) => void;
}
export declare function CronDialog({ open, onOpenChange }: CronDialogProps): import("react/jsx-runtime").JSX.Element;
export {};
